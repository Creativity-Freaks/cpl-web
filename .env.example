# Copy this file to .env and set your Supabase project credentials
# Vite exposes only variables prefixed with VITE_
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Admin gating (client-side)
# Either a single email or a comma-separated list
# VITE_ADMIN_EMAIL=
# VITE_ADMIN_EMAILS=
# Optional: any email at this domain treated as admin
# VITE_ADMIN_DOMAIN=

# Simple client-only auth (bypass Supabase) — set to "true" to enable
# VITE_SIMPLE_AUTH=false
# If using simple auth and you want an admin role, set a shared admin password
# VITE_ADMIN_PASSWORD=

# Server-only (do NOT expose to client): Service Role Key for admin scripts (required for seeding profiles)
# WARNING: Keep this key secret and never expose it to the client-side code.
SUPABASE_SERVICE_ROLE_KEY=

# Storage
# Client-side bucket name (used by browser code)
# VITE_SUPABASE_AVATARS_BUCKET=avatars
# Server-side bucket name (used by scripts)
# SUPABASE_AVATAR_BUCKET=avatars
